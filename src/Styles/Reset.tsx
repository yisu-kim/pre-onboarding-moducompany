import { css } from '@emotion/react';

const Reset = css`
  * {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
  }

  button {
    border: none;
    background: none;
  }
`;

export default Reset;
